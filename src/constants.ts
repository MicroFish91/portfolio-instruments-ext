import { l10n } from "vscode";

export const orderKeyPrefix: string = 'pi-order:';
export const tokenStorageKey: string = 'pi-token-storage';

export const reordererContext: string = 'reorderer';
export const reorderableContext: string = 'reorderable';
export const viewPropertiesContext: string = 'viewProperties';

export const operationCancelled: string = l10n.t('Operation cancelled');
